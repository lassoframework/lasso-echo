# Wave 0 Publisher Finding — LASSO Instagram Second Publisher

**Date:** 2026-08-26
**Spec ref:** ECHO_A_GRADE_SPEC.md §0.1
**Status:** SURFACED — awaiting Blake's tap to disconnect

---

## What the audit found

Between Aug 10 and Aug 26 the lassoframework public Instagram feed carried **65 posts**.
Echo's content_calendar accounts for **36** of them. The remaining **29 posts** include:

- Every dash/hyphen violation observed in the window (all 4 intraword hyphens, all em
  dashes that leaked to the feed).
- A repeating pattern firing near **14:10 ET on weekdays** whose captions have zero
  matching rows in `content_calendar`.

## Suspect: duplicate Zernio IG connection

`accounts_list` shows lassoframework Instagram connected **twice** under two account IDs:

| # | Account ID | Notes |
|---|-----------|-------|
| 1 | `6a69fc9cdf17280d93d0727f` | Primary — matches Echo's posting account |
| 2 | `6a74b3efd0fe733d1abc6fc1` | Duplicate — posts that do NOT appear in content_calendar |

Posts queried via `posts_list` for account ID `6a74b3efd0fe733d1abc6fc1` match the
unexplained 29: same 14:10 ET cadence, captions with dash violations, no rows in
`content_calendar`. This is the second publisher.

## Likely root cause

A legacy session (most likely a prior Zernio connect flow that created a second profile
binding) is still holding a live queue against the duplicate account connection.
Candidates in descending likelihood:

1. **Duplicate Zernio IG connection** (confirmed — two account IDs, one IG handle).
2. A legacy scheduler (Later, Buffer, or Meta native schedule) whose queue was never
   cleared when Echo took over the account.

## Recommendation

1. **Disconnect `6a74b3efd0fe733d1abc6fc1`** (the duplicate connection) in Zernio after
   Blake confirms it is not in active use for any other purpose.
2. Audit the Meta Business Suite native scheduling queue for the LASSO IG account and
   clear any residual scheduled posts there.
3. After disconnect: re-query `posts_list` for both IDs to confirm the 14:10 ET pattern
   stops. Monitor for one full week.

## Action needed from Blake

**Do not disconnect without Blake's explicit tap.**

- [ ] Blake: confirm ID `6a74b3efd0fe733d1abc6fc1` is the duplicate and authorize disconnect.
- [ ] Blake: confirm whether Meta native scheduler needs to be cleared separately.

This finding was posted to `#ops` via `ops_alerts.alert()` (see Wave 0 commit log).

---

*Generated by Wave 0 preflight audit. See ECHO_A_GRADE_SPEC.md §0.1 for full spec context.*
