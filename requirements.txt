requests>=2.31
slack_bolt>=1.18
boto3>=1.34
google-genai>=2.0  # Creative Studio (Gemini image gen); imported lazily in creative_studio.py
pypdf>=4.0  # Document intake PDF text extraction; imported lazily in doc_intake.py
Pillow>=10.0  # Intake ingest image convert/orientation/phash; imported lazily in intake_ingest.py
pillow-heif>=0.16  # HEIC to JPG for intake ingest; imported lazily in intake_ingest.py
reportlab>=4.0  # White label PDF reports; imported lazily in pdf_report.py
cryptography>=42.0  # GHL webhook Ed25519 signature verify; imported lazily in ghl_intake.py
faster-whisper>=1.0  # Clipper transcription; imported lazily in clipper.py
anthropic>=0.40  # Clipper moment scoring; imported lazily in clipper.py
opencv-python-headless>=4.10,<5  # Video editor caption face-avoidance; imported lazily in video_assets.py (optional, graceful fallback)
google-api-python-client>=2.100  # Podcast auto-ingest Drive pull; imported lazily in podcast_source.py
google-auth>=2.30  # Service-account creds for the Drive pull; imported lazily in podcast_source.py
higgsfield-client>=0.1  # Higgsfield headless b-roll rendering; imported lazily in higgsfield_renderer.py
httpx>=0.21,<0.29  # Required by higgsfield-client SDK

stripe>=9.0  # Welcome pipeline: read-only customers/subscriptions for new-client detection (imported lazily in welcome_posts.py)

cairosvg>=2.7  # Welcome logo pipeline: rasterize SVG gym logos to PNG (system cairo present on Railway); imported lazily in website_scan.py
